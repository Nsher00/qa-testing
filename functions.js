const returnTwo = ()=>{
    return 2
}

const greeting = (name)=>{
    return `Hello ${name}.`
}

const addNum = (num1,num2)=>{
    return num1 + num2
}

module.exports = {
    returnTwo,
    greeting,
    addNum
}